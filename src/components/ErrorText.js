import { styled } from "../stitches.config";

const ErrorText = styled('span', {
    fontSize: '0.875rem',
    lineHeight: '1.5rem',
    color: 'rgb(220 38 38)',
    marginTop: '6px',
});

export default ErrorText;